<Page x:Class="WindowsKontrolMerkezi.Pages.HealthWidgetPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      Background="#F5F5F5"
      Title="Sistem Sağlığı">
    <Border Padding="20">
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>

            <!-- Header -->
            <TextBlock Grid.Row="0" Text="Sistem Sağlığı" FontSize="32" FontWeight="Bold" 
                       Foreground="#1F1F1F" Margin="0,0,0,20"/>

            <!-- Content -->
            <ScrollViewer Grid.Row="1" VerticalScrollBarVisibility="Auto">
            <StackPanel>
                <!-- Overall Status Card -->
                <Border Background="White" CornerRadius="12" Padding="24" Margin="0,0,0,20">
                    <Border.Effect>
                        <DropShadowEffect BlurRadius="8" ShadowDepth="2" Opacity="0.1"/>
                    </Border.Effect>
                    <Grid>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="Auto"/>
                            <ColumnDefinition Width="*"/>
                        </Grid.ColumnDefinitions>

                        <!-- Status Icon -->
                        <TextBlock Grid.Column="0" Text="✓" FontSize="48" Foreground="#4CAF50" 
                                  Margin="0,0,20,0" VerticalAlignment="Center"/>

                        <!-- Status Text -->
                        <StackPanel Grid.Column="1" VerticalAlignment="Center">
                            <TextBlock Text="Sistem Durumu: Normal" FontSize="20" FontWeight="SemiBold" 
                                      Foreground="#1F1F1F" x:Name="StatusTitleBlock"/>
                            <TextBlock Text="Tüm sistem kaynakları normal seviyelerde çalışıyor" 
                                      FontSize="12" Foreground="#757575" Margin="0,4,0,0" 
                                      x:Name="StatusDescBlock"/>
                        </StackPanel>
                    </Grid>
                </Border>

                <!-- Disk Storage -->
                <Border Background="White" CornerRadius="12" Padding="24" Margin="0,0,0,20">
                    <Border.Effect>
                        <DropShadowEffect BlurRadius="8" ShadowDepth="2" Opacity="0.1"/>
                    </Border.Effect>
                    <Grid>
                        <Grid.RowDefinitions>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="Auto"/>
                        </Grid.RowDefinitions>

                        <!-- Header -->
                        <Grid Grid.Row="0" Margin="0,0,0,12">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="Auto"/>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="Auto"/>
                            </Grid.ColumnDefinitions>
                            <TextBlock Grid.Column="0" Text="💾" FontSize="16" Margin="0,0,8,0"/>
                            <TextBlock Grid.Column="1" Text="Disk Depolama" FontSize="16" FontWeight="SemiBold" 
                                      Foreground="#1F1F1F"/>
                            <TextBlock Grid.Column="2" x:Name="DiskPercentBlock" Text="0%" 
                                      FontSize="14" FontWeight="Bold" Foreground="#FF9800"/>
                        </Grid>

                        <!-- Progress Bar -->
                        <ProgressBar Grid.Row="1" x:Name="DiskProgressBar" Height="8" 
                                    Background="#FFE0B2" Foreground="#FF9800"
                                    Value="0" Maximum="100" Margin="0,0,0,8"/>

                        <!-- Details -->
                        <TextBlock Grid.Row="2" x:Name="DiskInfoBlock" Text="0 B / 0 B" 
                                  FontSize="11" Foreground="#9E9E9E"/>
                    </Grid>
                </Border>

                <!-- RAM Memory -->
                <Border Background="White" CornerRadius="12" Padding="24" Margin="0,0,0,20">
                    <Border.Effect>
                        <DropShadowEffect BlurRadius="8" ShadowDepth="2" Opacity="0.1"/>
                    </Border.Effect>
                    <Grid>
                        <Grid.RowDefinitions>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="Auto"/>
                        </Grid.RowDefinitions>

                        <!-- Header -->
                        <Grid Grid.Row="0" Margin="0,0,0,12">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="Auto"/>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="Auto"/>
                            </Grid.ColumnDefinitions>
                            <TextBlock Grid.Column="0" Text="🧠" FontSize="16" Margin="0,0,8,0"/>
                            <TextBlock Grid.Column="1" Text="RAM Belleği" FontSize="16" FontWeight="SemiBold" 
                                      Foreground="#1F1F1F"/>
                            <TextBlock Grid.Column="2" x:Name="RamPercentBlock" Text="0%" 
                                      FontSize="14" FontWeight="Bold" Foreground="#2196F3"/>
                        </Grid>

                        <!-- Progress Bar -->
                        <ProgressBar Grid.Row="1" x:Name="RamProgressBar" Height="8" 
                                    Background="#BBDEFB" Foreground="#2196F3"
                                    Value="0" Maximum="100" Margin="0,0,0,8"/>

                        <!-- Details -->
                        <TextBlock Grid.Row="2" x:Name="RamInfoBlock" Text="0 B / 0 B" 
                                  FontSize="11" Foreground="#9E9E9E"/>
                    </Grid>
                </Border>

                <!-- CPU -->
                <Border Background="White" CornerRadius="12" Padding="24" Margin="0,0,0,20">
                    <Border.Effect>
                        <DropShadowEffect BlurRadius="8" ShadowDepth="2" Opacity="0.1"/>
                    </Border.Effect>
                    <Grid>
                        <Grid.RowDefinitions>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="Auto"/>
                        </Grid.RowDefinitions>

                        <!-- Header -->
                        <Grid Grid.Row="0" Margin="0,0,0,12">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="Auto"/>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="Auto"/>
                            </Grid.ColumnDefinitions>
                            <TextBlock Grid.Column="0" Text="⚙️" FontSize="16" Margin="0,0,8,0"/>
                            <TextBlock Grid.Column="1" Text="CPU İşlemcisi" FontSize="16" FontWeight="SemiBold" 
                                      Foreground="#1F1F1F"/>
                            <TextBlock Grid.Column="2" x:Name="CpuPercentBlock" Text="0%" 
                                      FontSize="14" FontWeight="Bold" Foreground="#9C27B0"/>
                        </Grid>

                        <!-- Usage Progress Bar -->
                        <ProgressBar Grid.Row="1" x:Name="CpuProgressBar" Height="8" 
                                    Background="#E1BEE7" Foreground="#9C27B0"
                                    Value="0" Maximum="100" Margin="0,0,0,8"/>

                        <!-- Usage Info -->
                        <TextBlock Grid.Row="2" x:Name="CpuUsageBlock" Text="Kullanım: 0%" 
                                  FontSize="11" Foreground="#9E9E9E" Margin="0,0,0,6"/>

                        <!-- Temperature -->
                        <TextBlock Grid.Row="3" x:Name="CpuTempBlock" Text="Sıcaklık: 0°C" 
                                  FontSize="11" Foreground="#9E9E9E" Margin="0,0,0,6"/>

                        <!-- Status -->
                        <TextBlock Grid.Row="4" x:Name="CpuStatusBlock" Text="✓ Normal" 
                                  FontSize="11" Foreground="#4CAF50" FontWeight="SemiBold"/>
                    </Grid>
                </Border>

                <!-- GPU -->
                <Border Background="White" CornerRadius="12" Padding="24" Margin="0,0,0,20">
                    <Border.Effect>
                        <DropShadowEffect BlurRadius="8" ShadowDepth="2" Opacity="0.1"/>
                    </Border.Effect>
                    <Grid>
                        <Grid.RowDefinitions>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="Auto"/>
                        </Grid.RowDefinitions>

                        <!-- Header -->
                        <Grid Grid.Row="0" Margin="0,0,0,12">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="Auto"/>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="Auto"/>
                            </Grid.ColumnDefinitions>
                            <TextBlock Grid.Column="0" Text="🎮" FontSize="16" Margin="0,0,8,0"/>
                            <TextBlock Grid.Column="1" Text="GPU Grafik" FontSize="16" FontWeight="SemiBold" 
                                      Foreground="#1F1F1F"/>
                            <TextBlock Grid.Column="2" x:Name="GpuStatusIconBlock" Text="✓" 
                                      FontSize="16" Foreground="#4CAF50" FontWeight="Bold"/>
                        </Grid>

                        <!-- GPU Name -->
                        <TextBlock Grid.Row="1" x:Name="GpuNameBlock" Text="GPU Adı" 
                                  FontSize="12" Foreground="#424242" Margin="0,0,0,8"/>

                        <!-- GPU Usage -->
                        <TextBlock Grid.Row="2" x:Name="GpuUsageBlock" Text="Kullanım: 0%" 
                                  FontSize="11" Foreground="#9E9E9E" Margin="0,0,0,4"/>

                        <!-- GPU Temp -->
                        <TextBlock Grid.Row="3" x:Name="GpuTempBlock" Text="Sıcaklık: 0°C" 
                                  FontSize="11" Foreground="#9E9E9E"/>
                    </Grid>
                </Border>

                <!-- Storage Space Info -->
                <Border Background="White" CornerRadius="12" Padding="24" Margin="0,0,0,20">
                    <Border.Effect>
                        <DropShadowEffect BlurRadius="8" ShadowDepth="2" Opacity="0.1"/>
                    </Border.Effect>
                    <Grid>
                        <Grid.RowDefinitions>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="Auto"/>
                        </Grid.RowDefinitions>

                        <TextBlock Grid.Row="0" Text="Depolama Bilgisi" FontSize="14" FontWeight="SemiBold" 
                                  Foreground="#1F1F1F" Margin="0,0,0,12"/>

                        <TextBlock Grid.Row="1" x:Name="StorageBlock" Text="Toplam: 0 GB | Boş: 0 GB" 
                                  FontSize="12" Foreground="#424242" FontFamily="Consolas"/>
                    </Grid>
                </Border>

                <!-- Refresh Info -->
                <TextBlock Text="Veriler her 2 saniyede bir güncelleniyor" 
                          FontSize="11" Foreground="#9E9E9E" TextAlignment="Center" Margin="0,10,0,0"/>
            </StackPanel>
        </ScrollViewer>
        </Grid>
    </Border>
</Page>
